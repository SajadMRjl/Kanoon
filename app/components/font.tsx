import { Vazirmatn } from "next/font/google";
import localFont from "next/font/local";

export const estedad = localFont({ src: "../../public/Estedad-Regular.ttf" });

export const vazirmatn = Vazirmatn({ subsets: ["arabic"] });
